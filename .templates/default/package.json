{
  "name": "@tscli/demo",
  "version": "0.1.0",
  "description": "TypeScript CLI generated demo project",
  "main": "src/main.ts",
  "config": {
    "main": "build/main.js",
    "doc_folder": "docs/typedoc"
  },
  "scripts": {
    "build": "cross-env NODE_ENV=prod tsc",
    "check": "tslint -t codeFrame \"src/**/*.ts\"",
    "clean": "shx rm -rf build",
    "dev": "node $npm_package_config_main",
    "doc": "typedoc --module commonjs --target ES5 --ignoreCompilerErrors --exclude node_modules --out $npm_package_config_doc_folder src",
    "format": "tsfmt -r --baseDir ./",
    "node:debug": "node --inspect --debug-brk $npm_package_config_main",
    "prebuild": "run-s format check clean",
    "precommit": "run-s format check",
    "predev": "cross-env NODE_ENV=dev run-s clean tsc",
    "predoc": "shx rm -rf $npm_package_config_doc_folder && shx mkdir -p $npm_package_config_doc_folder",
    "prepush": "run-s test",
    "prestart": "run-s build",
    "pretest": "run-s build",
    "start": "node $npm_package_config_main",
    "test": "tape \"build/**/*/test.*.js\"|tap-summary",
    "test:web": "browserify \"build/**/*/test.*.js\"|tape-run|tap-summary",
    "tsc": "tsc",
    "web": "browser-sync start --config bs-config.js"
  },
  "keywords": [
    "javascript",
    "js",
    "ts",
    "typescript"
  ],
  "author": "[Full name] <email-inside-brackets> (project-github-repo-inside bracker)",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/<github-user-id>/<project-name>"
  },
  "devDependencies": {
    "@types/core-js": "^0.9.35",
    "@types/node": "^7.0.5",
    "@types/tape": "^4.2.29",
    "browser-sync": "^2.18.8",
    "browserify": "^14.1.0",
    "core-js": "^2.4.1",
    "cross-env": "^3.1.4",
    "cross-var": "^1.0.3",
    "husky": "^0.13.2",
    "nodemon": "^1.11.0",
    "npm-run-all": "^4.0.2",
    "shx": "^0.2.2",
    "tachyons": "^4.6.2",
    "tap-summary": "^3.0.1",
    "tape": "^4.6.3",
    "tape-run": "^2.2.1",
    "tslint": "^4.4.2",
    "typedoc": "^0.5.7",
    "typescript": "^2.2.1",
    "typescript-formatter": "^5.1.0"
  },
  "dependencies": {
    "bunyan": "^1.8.8"
  }
}
